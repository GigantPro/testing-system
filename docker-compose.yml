version: '3'

services:
  postgres:
    image: postgres
    env_file:
      - ./.env/database.env
    volumes:
      - postgres_data:/var/lib/posgresql/data
  backend:
    depends_on:
      - postgres
    environment:
      - DEBUG=false
    env_file: 
      - ./.env/backend.env
    build:
      context: backend
      dockerfile: Dockerfile
    volumes:
      - /var/log/xiver/testing-system/backend:/app/log
  frontend:
    build:
      dockerfile: Dockerfile
      context: frontend
  nginx:
    depends_on:
      - backend
    build:
      dockerfile: Dockerfile
      context: nginx
    ports:
      - 80:80

volumes:
  postgres_data:
