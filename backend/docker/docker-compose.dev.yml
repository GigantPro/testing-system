version: '3'

services:
  ts-backend-dev:
    environment:
      - DEBUG=true
    env_file: 
      - ./../.env.dev
    build:
      context: ../
      dockerfile: docker/Dockerfile.dev
    volumes:
      - ../src:/app/src
    ports:
      - "5001:5001"
